import { User, Message } from "discord.js";

export const handleRolesAssignment = (user: User, msg: Message) => {
  // Your role assignment logic here...
  // Add roles based on answers to user
  // Send a final message to the HEY_CHANNEL_ID with user presentation
};
